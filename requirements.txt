# --- Web & API (FastAPI) ---
fastapi==0.104.1
uvicorn[standard]==0.24.0   # 고성능 웹 서버
python-multipart==0.0.6     # 파일 업로드 처리용
pydantic[email]==2.5.2      # 데이터 검증 및 설정 관리

# --- Scraping ---
beautifulsoup4==4.12.2
requests==2.31.0

# --- Database & Vector Search (PostgreSQL) ---
sqlalchemy==2.0.23          # ORM (파이썬 코드로 DB 조작)
psycopg2-binary==2.9.9      # PostgreSQL 드라이버
pgvector==0.2.4             # 벡터 검색 확장 도구 연동

# --- Data Science & AI (Hugging Face / ML) ---
pandas==2.1.3               # 데이터 프레임 처리
numpy<2.0.0                 # 수치 연산
torch==2.1.1                # 딥러닝 프레임워크 (PyTorch)
transformers==4.35.2        # Hugging Face 모델 사용
sentence-transformers>=2.3.0 # 텍스트 벡터화(Embedding)용
huggingface-hub>=0.20.0     # 모델을 다운로드하기 위한 통신 도구
scikit-learn==1.3.2         # 머신러닝 및 평가 도구
shap==0.43.0                # XAI (설명 가능한 AI) 구현

# --- MLOps & Experiment (MLflow) ---
mlflow==2.8.1               # 실험 기록 및 모델 관리

# --- Environment & Security ---
python-dotenv==1.0.0        # .env 파일 로드용
passlib[bcrypt]==1.7.4      # 비밀번호 암호화
python-jose[cryptography]==3.3.0 # JWT 토큰 인증용